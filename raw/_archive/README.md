# raw/_archive/ — processed captures  ·  ⚠️ GIT-IGNORED

Where files from `raw/` land after the agent has translated them into `wiki/`. A local record of what's been handled.

- **Distinct from the repo's `archives/`** (which holds deprecated AIOS files). This holds processed *captures* and is git-ignored because the originals may contain sensitive material.
- **Move, never delete.** Never modify a file after it lands here — it's a permanent local record.
- If you ever clear this folder, that's fine — the durable knowledge lives in `wiki/` (committed). Originals needing durable retention belong in the your sanctioned cloud, not git.

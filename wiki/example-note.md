# Example note

A sample wiki entry showing the shape: **one topic per file**, kebab-case filename, plain interpreted knowledge in your voice, cross-linked to related entries. The `/wiki` skill writes these from `raw/` captures and updates the [index](_index.md).

Replace this with your own knowledge and remove it from the index. The lint gate (`tools/wiki_lint.py`) checks links and scans for accidental secrets/confidential leakage before commit.
